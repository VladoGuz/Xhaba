/**
 * Nota didáctica:
 * En la arquitectura actual de Xhaba, la declaración del árbol de rutas de la interfaz
 * se encuentra centralizada y renderizada directamente dentro de client/src/App.jsx.
 * 
 * Este archivo (AppRouter.jsx) se mantiene vacío en caso de que a futuro se decida refactorizar
 * y desacoplar la lógica de enrutamiento a un archivo independiente del layout principal.
 */
export default function AppRouter() {
  return null;
}