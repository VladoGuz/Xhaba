import React from 'react';

/**
 * Componente CartItem (Item de Carrito).
 * 
 * NOTA PEDAGÓGICA: Este componente está disponible como plantilla para futuras refactorizaciones.
 * Actualmente, los elementos del carrito se renderizan directamente de forma inline dentro de `Cart.jsx`
 * para simplificar la cohesión del diseño y evitar la sobrecarga de propiedades (prop drilling) en el layout.
 * 
 * Si se desea reactivar, este componente debería recibir:
 * - `item`: Objeto que representa la prenda en el carrito ({ id, title, price, quantity, image, color, size }).
 * - `onRemove`: Función disparadora para remover el ítem.
 */
function CartItem() {
  return null;
}

export default CartItem;
